# UUID of the query this response corresponds to
string uuid

# The query text that was sent
string query

# The image data (base64 or file path depending on service used)
string image_info

# The LLM response text
string response

# Timestamp when response was received
time timestamp
